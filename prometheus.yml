global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "sensor"
    metrics_path: "/metrics"
    scrape_timeout: 10s
    static_configs:
      - targets: ["sensor-service:8000"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
